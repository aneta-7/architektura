<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
       <%= image_tag gravatar_url(@user, 200) %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
