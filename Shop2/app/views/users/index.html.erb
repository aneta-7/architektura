<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag gravatar_url(user, 100) %>      
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

