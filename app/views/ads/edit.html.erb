<% provide(:title, "Edit") %>
<% provide(:buton_text, 'Update ad') %>

<%if logged_in? %>

<h1> Editing Ad </h1>

<div class="row">
    <div class = "col-md-6 col-md-offset-3">
	<%=render 'form', ad: @ad %>
    </div>
</div>
<% end %>

<%= link_to 'Show', @ad, class: "btn btn-lg btn-default" %> |
<%= link_to 'Back', ads_path, class: "btn btn-default btn-outline glyphicon glyphicon-arrow-left" %>

